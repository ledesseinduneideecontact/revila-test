# Create a smoother hover animation using CSS motion path (offset-path) for continuous/accelerated movement,
# and replace the blank card with a realistic photo look (gloss, paper grain, framed print).
html = r"""<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>REVELO — NFC Hover (Motion Path + Real Photo)</title>
<style>
  :root{
    --ink:#0f172a;
    --muted:#64748b;
    --accent:#58cc02;
    --nfc:#3b82f6;
    --panel:#ffffff;
    --bg-start:#ffffff;
    --bg-end:#eef3ff;
    --shadow-strong: 0 28px 90px rgba(15,23,42,.28);
    --shadow: 0 16px 48px rgba(15,23,42,.16);
    --shadow-soft: 0 6px 18px rgba(15,23,42,.10);
  }

  html,body{height:100%;margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,Arial;background:radial-gradient(1200px 800px at 50% -10%,var(--bg-start),var(--bg-end));color:var(--ink);}
  .demo{ width: 860px; max-width: 96vw; aspect-ratio:16/9; background:linear-gradient(180deg,#fff,#fafcff);
         border-radius:24px; box-shadow: var(--shadow); margin:auto; position:relative; overflow:hidden; perspective:1600px;
         transition: transform .7s cubic-bezier(.2,.7,.2,1), box-shadow .7s; }
  .demo:hover{ transform: rotateX(2deg) rotateY(-1.5deg); box-shadow: var(--shadow-strong); }

  /* ---------- Printed photo (realistic) ---------- */
  .photo-wrap{
    position:absolute; left: 8%; top: 22%;
    width: 470px; height: 310px;
    transform: rotate(-2deg);
  }
  .frame{
    position:absolute; inset:-16px; border-radius:22px; background:#fff;
    box-shadow: inset 0 0 0 1px rgba(0,0,0,.07), 0 14px 36px rgba(0,0,0,.12);
  }
  .photo{
    position:absolute; inset:0; border-radius:18px; overflow:hidden;
    background:
      /* gloss */ linear-gradient(190deg, rgba(255,255,255,.35) 0%, rgba(255,255,255,0) 40%),
      /* image */ url('data:image/svg+xml;utf8,\
      <svg xmlns="http://www.w3.org/2000/svg" width="470" height="310">\
        <defs>\
          <linearGradient id="sky" x1="0" y1="0" x2="0" y2="1">\
            <stop offset="0%" stop-color="%23bfe5ff"/>\
            <stop offset="65%" stop-color="%2386d5ff"/>\
            <stop offset="100%" stop-color="%2368c1ff"/>\
          </linearGradient>\
          <linearGradient id="water" x1="0" y1="0" x2="0" y2="1">\
            <stop offset="0%" stop-color="%236ecbff"/>\
            <stop offset="100%" stop-color="%2357b8f5"/>\
          </linearGradient>\
        </defs>\
        <rect width="470" height="310" fill="url(%23sky)"/>\
        <circle cx="392" cy="54" r="12" fill="%23ffd54a"/>\
        <g fill="%2387c79a">\
          <polygon points="30,180 110,95 190,180" />\
          <polygon points="120,178 200,86 280,178" />\
          <polygon points="210,182 300,110 390,182" />\
        </g>\
        <rect x="0" y="190" width="470" height="120" fill="url(%23water)" />\
        <g fill="%239fe0b7">\
          <rect x="24" y="206" width="86" height="72" rx="12"/>\
          <rect x="120" y="212" width="98" height="66" rx="12"/>\
          <rect x="226" y="206" width="86" height="72" rx="12"/>\
        </g>\
      </svg>');
    background-size: cover; box-shadow: inset 0 0 0 0 rgba(0,0,0,0);
  }
  /* paper grain */
  .grain{ position:absolute; inset:0; pointer-events:none; mix-blend-mode: multiply; opacity:.08;
          background-image: repeating-linear-gradient(0deg, rgba(0,0,0,.04) 0 1px, transparent 1px 2px),
                            repeating-linear-gradient(90deg, rgba(0,0,0,.02) 0 1px, transparent 1px 3px); }

  /* NFC glow from bottom-right corner (no circle) */
  .nfc-glow{ position:absolute; right:-6px; bottom:-6px; width: 200px; height: 200px; pointer-events:none;
             background: radial-gradient(140px 140px at 100% 100%, rgba(59,130,246,.25), rgba(59,130,246,0));
             opacity:0; filter: blur(2px); }

  /* ---------- Phone with motion path ---------- */
  .phone{
    position:absolute; width: 300px; height: 600px; border-radius: 38px; background:#0b1428;
    box-shadow: 0 34px 70px rgba(5,10,30,.35), inset 0 0 0 2px rgba(255,255,255,.06);
    display:grid; place-items:center;
    offset-path: path("M 820,120 C 700,180 540,220 470,260 C 420,290 380,320 360,350");
    offset-distance: 0%;
    offset-rotate: 0deg;
    transform: rotate(12deg);
  }
  .screen{ width: 266px; height: 566px; border-radius: 32px; overflow:hidden; background: linear-gradient(180deg,#eaf2ff,#f7fbff); position:relative; }
  .gallery{ display:grid; grid-template-columns: repeat(3,1fr); gap:12px; padding:18px; }
  .thumb{ height:76px; border-radius: 12px; background:#cfe7ff; }
  .thumb:nth-child(2){ background:#c8f5d4 } .thumb:nth-child(3){ background:#ffd7e8 }
  .thumb:nth-child(4){ background:#f8e0a6 } .thumb:nth-child(5){ background:#cfe7ff }
  .thumb:nth-child(6){ background:#c8f5d4 } .thumb:nth-child(7){ background:#ffd7e8 }
  .thumb:nth-child(8){ background:#f8e0a6 } .thumb:nth-child(9){ background:#cfe7ff }
  .cta{ position:absolute; left:16px; right:16px; bottom:16px; height:50px; border-radius: 25px; background: var(--accent);
        box-shadow: 0 10px 24px rgba(88,204,2,.35); }

  /* scanning line + toast */
  .scan{ position:absolute; left:10%; width:80%; height:2px; top: 96px;
         background: linear-gradient(90deg, transparent, rgba(59,130,246,.9), transparent); border-radius:2px;
         opacity:0; filter: blur(.2px); }
  .toast{ position:absolute; left:14px; right:14px; top:16px; min-height:58px; border-radius:16px; background: rgba(15,23,42,.9); color:#fff;
          display:flex; align-items:center; gap:10px; padding:10px 14px; transform: translateY(-90px); opacity:0; }
  .dot{ width:14px; height:14px; border-radius:50%; background:#60a5fa; box-shadow:0 0 12px rgba(96,165,250,.9); }
  .label{ font: 600 14px/1 Inter,ui-sans-serif; opacity:.95; }
  .small{ font:500 12px/1 Inter,ui-sans-serif; opacity:.7; margin-left:auto; }

  /* ---------- Hover sequence ---------- */
  .demo:hover .phone,
  .demo.touch-active .phone{ animation: moveAlong 1.2s cubic-bezier(.32,0,.67,0) forwards; } /* accelerated (ease-in) */
  .demo:hover .nfc-glow,
  .demo.touch-active .nfc-glow{ animation: glow 1s ease-out .9s forwards; }
  .demo:hover .scan,
  .demo.touch-active .scan{ animation: scanning .85s ease-in 1s 1 forwards; }
  .demo:hover .toast,
  .demo.touch-active .toast{ animation: toast-in .6s cubic-bezier(.2,.7,.2,1) 1.55s forwards; }
  .demo:hover .photo-wrap,
  .demo.touch-active .photo-wrap{ animation: lift 1s ease-out forwards; }

  @keyframes moveAlong{ from{ offset-distance: 0%; transform: rotate(12deg);} to{ offset-distance: 100%; transform: rotate(-6deg);} }
  @keyframes lift{ from{ transform: rotate(-2deg) translateZ(0);} to{ transform: rotate(-2deg) translate3d(0,-3px,0);} }
  @keyframes glow{ 0%{ opacity:0; transform: scale(.9); } 45%{ opacity:.75; transform: scale(1.02);} 100%{ opacity:0; transform: scale(1.3);} }
  @keyframes scanning{ 0%{ opacity:0; transform: translateY(-16px);} 10%{ opacity:1;} 100%{ opacity:0; transform: translateY(230px);} }
  @keyframes toast-in{ 0%{ transform: translateY(-90px); opacity:0;} 60%{ transform: translateY(6px); opacity:1;} 100%{ transform: translateY(0); opacity:1;} }

  .hint{ position:absolute; bottom:14px; left:16px; background: rgba(255,255,255,.86); border-radius:999px; padding:6px 10px; font-size:12px; color:var(--muted); box-shadow: var(--shadow-soft); }

  @media (prefers-reduced-motion: reduce){
    .demo, .demo *{ animation:none !important; transition:none !important; }
  }
</style>
</head>
<body>

<div class="demo" id="demo" aria-label="Hover : le téléphone détecte le NFC sur une photo imprimée">
  <div class="photo-wrap">
    <div class="frame"></div>
    <div class="photo">
      <div class="grain"></div>
      <div class="nfc-glow"></div>
    </div>
  </div>

  <div class="phone" aria-hidden="true">
    <div class="screen">
      <div class="toast"><span class="dot"></span><span class="label">NFC détecté</span><span class="small">Prêt</span></div>
      <div class="scan"></div>
      <div class="gallery">
        <div class="thumb"></div><div class="thumb"></div><div class="thumb"></div>
        <div class="thumb"></div><div class="thumb"></div><div class="thumb"></div>
        <div class="thumb"></div><div class="thumb"></div><div class="thumb"></div>
      </div>
      <div class="cta"></div>
    </div>
  </div>

  <div class="hint">Survolez (ou touchez) l'animation</div>
</div>

<script>
  // Support tactile : tap pour rejouer
  (function(){
    const box = document.getElementById('demo');
    function play(){
      box.classList.remove('touch-active'); void box.offsetWidth; box.classList.add('touch-active');
      setTimeout(()=> box.classList.remove('touch-active'), 2200);
    }
    box.addEventListener('click', function(e){ e.preventDefault(); play(); }, {passive:false});
  })();
</script>

</body>
</html>
"""
with open("/mnt/data/revelo-nfc-hover-motionpath.html","w", encoding="utf-8") as f:
    f.write(html)
print("Saved to /mnt/data/revelo-nfc-hover-motionpath.html")
